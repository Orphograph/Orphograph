# dormant/

Built, not served. Nothing under this directory is copied into the image
(the Dockerfile copies `server/`, `web/`, `scripts/` and `content/` only), so
none of it is reachable on the site. Pages here are kept so the work is not
lost; moving one back under `web/` is a deliberate act.
