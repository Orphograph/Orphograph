# Ledger verification — 2026-09-13

The endpoint smoke probe now defaults to GET/HEAD only, reports omitted write probes,
and requires `--allow-writes` before sending POSTs. Four canonical sample routes
exercise valid input; receipt JSON is checked against the saved id and hash.
All four content checks returned HTTP 200 against production on September 13.

The layout sweep measured visible capped children and their actual parent geometry
at desktop widths, distinguishing grid cells and deliberately left-aligned copy
from centered compositions. At 1920px, features measured 1180px with 290px/290px
margins, the arch 460px with 626px/626px, and the sample card 680px with 138px/138px.
At 2560px the corresponding margins were 610/610, 946/946, and 326.56/326.56.
At 390px the measures shrink to 340, 292, and 292px with equal 1px border offsets.
No document overflow was observed at these widths. The sample card is now included
in MEASURED; its cap and the existing two caps have cross-source mutation controls.
No additional unintended stranding was found. Left-aligned prose, the drop target,
decorative overflow, flex children and grid columns are not centered by this check.

Five unused hero trust rules and the unused reassure rule were removed. Their
stylesheet hashes, versions and references were bumped, including server templates.
The existing card-notify test now reads the pinned version rather than rejecting a
legitimate version increment.

Review of previously merged PRs 242 and 243: inspected each merge against its first
parent. The endpoint repair preserves case-insensitive response headers and quotes
paths without altering query separators. Error-header injection resets its flag in
a finally block and clears it before emitting headers, avoiding duplicate insertion.
Their focused tests passed. No additional correctness finding was confirmed in those
merged diffs; this is a new review by Codex, not a claimed legacy review-tool result.

The previously local route enumeration/classifier now ships in scripts/ and runs
after a successful production deploy. It sends GET/HEAD only; findings and unknown
answers fail the CI gate. The test controls include a deliberately broken header,
method mismatch, and the CLI exit code. CodeQL is configured with an immutable
upstream action commit for Python and JavaScript/TypeScript. Configuration alone
is not evidence of a successful hosted scan or sustained operational maturity.

The live route sweep crossed a per-IP quota boundary on `/api/stripe/session`
(GET 400, HEAD 429). An asymmetric 429 now reports UNKNOWN, not a confirmed
method-parity defect. This remains a failing result under CI's zero-unknown
budget; no throttle result is silently converted to a passing handler check.
