[pytest]
# Pins rootdir to the repo root so the root conftest.py (skip gate) loads for
# every invocation, including `pytest tests/` alone. Deliberately no other
# options: tests/test_repo_hygiene.py assumes pytest's default python_files;
# if an option is ever added here, mirror it there.
